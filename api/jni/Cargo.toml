[package]
name = "nova_jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = { version = "0.21", features = ["invocation"] }
nova_ffi = { path = "../ffi" }
nova_screen = { path = "../../modules/screen" }
nova_input = { path = "../../modules/input" }
